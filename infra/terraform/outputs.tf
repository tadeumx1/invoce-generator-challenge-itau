# Root-level outputs are surfaced by T4 (api_endpoint comes from the api-gateway module).
# Network-level outputs remain inside the network module so consumers reference them
# via module.network.<output>.
